# Security - CodeQL and Dependency Review
name: Security

on:
  push:
    branches: [main]
  pull_request:
    types: [opened, synchronize]
  schedule:
    - cron: '0 6 * * 1'  # Weekly Monday 6am UTC

concurrency:
  group: security-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  security-events: write

jobs:
  codeql:
    name: CodeQL Analysis
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6.0.1

      - name: Initialize CodeQL
        uses: github/codeql-action/init@7c9a7896f03bb1f3de14c5663ed46759e27443e0 # v4.31.9
        with:
          languages: javascript-typescript
          queries: security-and-quality

      - name: Autobuild
        uses: github/codeql-action/autobuild@7c9a7896f03bb1f3de14c5663ed46759e27443e0 # v4.31.9

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@7c9a7896f03bb1f3de14c5663ed46759e27443e0 # v4.31.9
        with:
          category: "/language:javascript-typescript"
          output: codeql-results
          upload: true

      - name: Upload CodeQL results
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6.0.0
        if: always()
        with:
          name: codeql-results
          path: codeql-results/
          retention-days: 7

  dependency-review:
    name: Dependency Review
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    # Non-blocking until Dependency Graph is enabled in repository settings
    # See: https://github.com/jbcom/nodejs-strata/settings/security_analysis
    continue-on-error: true
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6.0.1

      - name: Dependency Review
        uses: actions/dependency-review-action@3c4e3dcb1aa7874d2c16be7d79418e9b7efd6261 # v4.8.2
        # Continue on error - dependency graph must be enabled in repo settings
        # See: https://github.com/jbcom/nodejs-strata/settings/security_analysis
        continue-on-error: true
        with:
          fail-on-severity: high
          comment-summary-in-pr: always
